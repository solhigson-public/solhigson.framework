# Internationalization (i18n)

All user-facing text MUST use resource-based string localization. MUST use culture-aware date and number formatting. MUST NOT use culture-based currency formatting — currency MUST be explicit per transaction.

For implementation patterns, MUST invoke the `i18n` skill.
