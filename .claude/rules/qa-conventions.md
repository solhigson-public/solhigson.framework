# QA Test Script Conventions

MUST use ID format `TC-{AREA}-{NNN}`. MUST have 15 steps max per test case. MUST use concrete test data values. Scripts: `.claude/qa/`, template: `.claude/templates/qa-test-script-template.md`.

For structure rules, test execution tiers, and full conventions, MUST invoke the `development-workflow` skill.
