---
name: string-constants
description: String constant patterns — what must/must not be constants, .NET file locations, parameterized factory methods, and test assertion conventions.
---

# String Constants Skill

Centralized string constant patterns across all layers. MUST follow `reference.md` for classification rules and code patterns.

## When This Skill Is Invoked
- When adding error messages, user messages, or validation strings
- When adding email template placeholders
- When writing test assertions against service messages
- When deciding if a string literal needs a constant
